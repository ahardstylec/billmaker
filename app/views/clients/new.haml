#new_client_container
	#new_client_content
		%nav
			=button_to "zurück", url(:clients, :index), method: :get, class: :button
		%h4="Neuer Client"
		-form_for @client, url(:clients, :create) do |f|
			=partial "clients/form", locals: {f: f}